# ME-RUN15 empty guard note

The command requires exactly one explicit ticker input mode.

Accepted modes:

* explicit comma-separated tickers;
* local ticker file;
* local cached-source discovery.

The command returns exit code 2 when ticker input is invalid or the cached-source batch runtime rejects the request.
