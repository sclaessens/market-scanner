# Sprint 1 Technical Lead Developer Specification — Structure Classification Alignment & Governance Refinement

## 1. Sprint Status

Status: IMPLEMENTATION COMPLETE / CERTIFIED COMPLETE

Closeout note: Sprint 1 is formally certified complete. See `docs/sprints/sprint_1_closeout.md`.

Sprint 1 Institutional Re-Alignment Audit is complete.

Sprint 1 Documentation Correction is complete.

Sprint 1 Execution Plan is complete.

This document is the official Technical Lead developer specification for Sprint 1. It is implementation-safe and scope-constrained.

## 2. Governance Inheritance

Sprint 1 inherits the certified Sprint 0 doctrine:

- classification upstream
- allocation downstream
- Decision Engine = ONLY allocation authority
- upstream layers classify only
- reporting communicates only
- no upstream tradeability
- no hidden filtering
- no hidden allocation semantics outside Decision Engine

Validation Layer may classify structural coherence only.

Validation Layer may not determine tradeability, allocation eligibility, conviction, urgency, actionability, execution readiness, or BUY/SELL/HOLD/TRIM/REMOVE behavior.

## 3. Technical Objective

Align and verify the Validation Layer runtime contract around:

- `structure_state`
- `structure_reason`

Confirm deprecated compatibility aliases remain structure-only:

- `valid_setup`
- `validation_reason`

The sprint objective is contract stabilization, compatibility cleanup, schema enforcement, tests, and governance checks. It is not a Validation Layer reconstruction sprint.

## 4. Technical Lead Implementation Philosophy

The developer must:

1. FIRST inspect whether runtime implementation already satisfies Sprint 1 governance.
2. ONLY modify runtime code if governance gaps are proven.
3. Prefer minimal contract-alignment changes over rewrites.
4. Avoid broad Validation Layer reconstruction.
5. Preserve deterministic outputs.
6. Preserve opportunity distribution.
7. Preserve current runtime behavior unless governance misalignment is proven.

If inspection confirms current runtime and tests already satisfy the contract, the developer should limit work to tests, CI/governance checks, or documentation updates if those are still needed.

## 5. Runtime Inspection Requirements

Inspect before editing:

- `scripts/core/build_validation_layer.py`
- `tests/core/test_build_validation_layer.py`
- `tests/core/test_entry_quality.py`
- `data/processed/validation_layer.csv`
- `data/processed/entry_quality_metrics.csv`

Confirm:

- `validation_layer.csv` has `structure_state` and `structure_reason`
- `valid_setup` is structure-only compatibility alias
- `validation_reason` is structure-only compatibility alias
- Validation Layer does not emit forbidden runtime fields
- entry quality metrics are descriptive metadata only
- extension, volume quality, breakout quality, and RR quality do not gate structure classification

## 6. Allowed Implementation Scope

Only the following areas may be modified if required:

- `scripts/core/build_validation_layer.py`
- validation-related tests under `tests/core/`
- CI/governance enforcement configuration, if present and required
- Sprint 1 documentation files

Runtime code may be modified only if inspection proves a governance gap.

## 7. Explicitly Forbidden Implementation Scope

Do not modify:

- scanner logic
- context logic
- watchlist logic
- portfolio logic
- Decision Engine logic
- reporting logic
- Telegram logic
- strategy scoring
- trading thresholds
- allocation logic

Do not change:

- BUY/SELL/HOLD/TRIM/REMOVE behavior
- Decision Engine final-action behavior
- scanner ranking or output distribution
- context leadership classification
- watchlist timing-state behavior
- portfolio risk/exposure behavior
- reporting presentation or filtering

## 8. Exact Files Allowed To Change

Allowed if required:

- `scripts/core/build_validation_layer.py`
- `tests/core/test_build_validation_layer.py`
- `tests/core/test_entry_quality.py`
- `tests/core/test_build_entry_quality_backfill.py`, only if compatibility with descriptive metadata requires test alignment
- `docs/sprints/sprint_1_structure_classification.md`
- `docs/sprints/sprint_1_execution_plan.md`
- `docs/sprints/sprint_1_developer_spec.md`
- CI/governance configuration files, only if present and directly needed for Sprint 1 checks

## 9. Exact Files Forbidden To Change

Forbidden unless a separate Technical Lead approval expands scope:

- `scripts/core/scanner.py`
- `scripts/core/build_context_layer.py`
- `scripts/core/build_context_backfill.py`
- `scripts/watchlist/`
- `scripts/portfolio/`
- `scripts/core/decision_engine.py`
- `scripts/reporting/`
- `scripts/telegram/`
- data contracts outside Validation Layer outputs

Generated CSV files may be regenerated by validation commands. They must not be blindly committed.

## 10. Required Runtime Contract

Primary Validation Layer contract:

- `structure_state`
- `structure_reason`

Deprecated compatibility aliases only:

- `valid_setup`
- `validation_reason`

Forbidden semantics:

- `VALID` / `INVALID` semantics
- tradeability semantics
- allocation semantics
- conviction semantics
- urgency semantics
- execution-readiness semantics
- BUY/SELL/HOLD/TRIM/REMOVE semantics

Allowed structure-state vocabulary:

- `COHERENT`
- `BROKEN`
- `INCOMPLETE`
- `MISSING_DATA`, if required
- `DEGRADED_STRUCTURE`, only if explicitly defined as descriptive structure metadata

## 11. Required Schema Expectations

`validation_layer.csv` must expose structure-classification fields only.

Expected fields:

- `ticker`
- `date`
- `structure_state`
- `structure_reason`
- `setup_type`
- `valid_setup`, compatibility alias only
- `validation_reason`, compatibility alias only

`entry_quality_metrics.csv`, if emitted, must remain descriptive metadata only.

Expected descriptive metadata fields may include:

- `entry_quality_state`
- `entry_quality_reason`
- extension metrics
- volume metrics
- range metrics
- breakout-distance metrics

## 12. Compatibility Alias Rules

`valid_setup`:

- may remain temporarily
- must mean structure coherence only
- must not mean tradeability
- must not mean allocation eligibility
- must not mean actionability
- must not block opportunities based on descriptive metadata

`validation_reason`:

- may remain temporarily
- must mirror or map to `structure_reason`
- must not contain allocation, action, urgency, rejection, or execution-readiness language

New code and tests should prefer `structure_state` and `structure_reason`.

## 13. Descriptive Metadata Governance Rules

The following are descriptive metadata only:

- entry quality
- extension
- volume quality
- breakout quality
- RR quality

These may not:

- change `structure_state`
- block `valid_setup` compatibility alias
- eliminate opportunities
- act as hidden filters
- imply tradeability
- imply allocation readiness
- imply execution readiness
- create action semantics

If tests discover metadata affects structure classification, that is a Sprint 1 governance defect.

## 14. Forbidden Field Rules

Validation Layer may not emit:

- `tradeable_setup`
- `context_tradeable`
- `tradeability`
- `conviction`
- `allocation_priority`
- `final_action`
- `urgency`
- `actionable`
- `BUY`
- `SELL`
- `HOLD`
- `TRIM`
- `REMOVE`

Validation Layer tests should assert absence of these fields where appropriate.

## 15. Required Implementation Sequence

1. Read `AGENTS.md`, Sprint 0 certification docs, Sprint 1 structure document, and Sprint 1 execution plan.
2. Inspect current Validation Layer implementation and tests.
3. Inspect current `validation_layer.csv` and `entry_quality_metrics.csv` headers.
4. Determine whether runtime code already satisfies Sprint 1 governance.
5. If runtime already satisfies governance, avoid runtime edits.
6. If gaps exist, make the smallest possible contract-alignment patch in allowed files only.
7. Add or update tests for the proven gap.
8. Run required tests.
9. Run required governance grep checks.
10. Run full pipeline only if runtime code changed.
11. Document results, grep interpretation, and any generated artifact risks.

## 16. Required Tests

Run:

```bash
.venv/bin/python3 -m pytest
```

Required test coverage:

- pytest validation tests
- schema assertions
- forbidden-field assertions
- deterministic output validation
- compatibility alias validation

Specific assertions should cover:

- `structure_state` is present and authoritative
- `structure_reason` is present and authoritative
- `valid_setup` remains structure-only compatibility alias
- `validation_reason` remains structure-only compatibility alias
- RR quality does not gate `structure_state`
- extension does not gate `structure_state`
- volume quality does not gate `structure_state`
- entry quality remains descriptive metadata only
- forbidden runtime fields are absent from validation output

## 17. Required Governance/Grep Checks

Run:

```bash
grep -R "tradeable_setup" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
grep -R "context_tradeable" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
grep -R "allocation_priority" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
grep -R "final_action" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
grep -R "urgency" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
grep -R "BUY" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
grep -R "SELL" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
grep -R "REMOVE" scripts/core/build_validation_layer.py tests/core/test_build_validation_layer.py
```

Expected interpretation:

- no active validation source logic emits forbidden fields
- test hits are acceptable only when asserting absence
- any source hit in Validation Layer must be reviewed as a possible governance defect

## 18. Runtime Validation Procedure

If runtime code changed, run:

```bash
PATH="/Users/stevenclaessens/market-scanner/.venv/bin:$PATH" python3 scripts/run_full_pipeline.py
```

Then inspect:

```bash
head -n 1 data/processed/validation_layer.csv
head -n 1 data/processed/entry_quality_metrics.csv
```

Expected:

- `validation_layer.csv` remains structure-classification only
- `entry_quality_metrics.csv` remains descriptive metadata only
- full pipeline passes
- generated data changes are documented and not blindly committed

If only tests or documentation changed, full pipeline is optional unless Technical Lead review requests it.

## 19. Acceptance Criteria

Sprint 1 implementation is acceptable only when:

- `structure_state` is authoritative
- `structure_reason` is authoritative
- `valid_setup` is compatibility-only
- `validation_reason` is compatibility-only
- no allocation leakage exists in Validation Layer
- no hidden filtering exists in Validation Layer
- descriptive metadata remains non-blocking
- forbidden runtime fields are absent
- tests pass
- governance checks pass
- Technical Lead review passes

## 20. Definition Of Done

Sprint 1 is done only when:

- inspection-first workflow was followed
- no broad Validation Layer reconstruction occurred
- runtime code changed only if a governance gap was proven
- deterministic outputs are preserved
- opportunity distribution is preserved
- current runtime behavior is preserved unless governance misalignment was proven
- tests pass
- grep checks are run and interpreted
- pipeline validation is run if runtime changed
- generated artifact risks are documented
- Technical Lead review confirms compliance
- Functional Analyst review confirms classification-first clarity

## 21. Risks And Controls

| Risk | Severity | Control |
|---|---|---|
| Developer rewrites Validation Layer unnecessarily | HIGH | Inspect first; patch only proven gaps |
| `valid_setup` becomes eligibility again | HIGH | Keep compatibility-only and structure-only |
| `validation_reason` becomes primary contract | MEDIUM | Prefer `structure_reason` in new code/tests |
| entry quality or extension becomes a gate | HIGH | Add/keep metadata-only tests |
| forbidden fields reappear in validation output | HIGH | Enforce grep and schema assertions |
| scope expands into Decision Engine or scanner | HIGH | Explicit forbidden files and behavior |
| generated data artifacts create confusion | MEDIUM | Inspect headers and document artifact hygiene |

## 22. Technical Lead Implementation Notes

Current inspection suggests `scripts/core/build_validation_layer.py` may already satisfy most Sprint 1 runtime requirements:

- `structure_state` and `structure_reason` are present
- `valid_setup` is commented as a deprecated compatibility alias
- `validation_reason` mirrors `structure_reason`
- `entry_quality_metrics.csv` is emitted separately as descriptive metadata
- tests already cover RR, extension, distance, and volume as non-gates

The developer must still verify this in the working tree before deciding whether runtime edits are necessary.

Likely Sprint 1 work may be limited to:

- strengthening tests for forbidden fields
- clarifying compatibility alias expectations
- adding CI/governance enforcement if configuration exists
- documenting validation results

Do not change Decision Engine behavior to satisfy Sprint 1.

## 23. Final Technical Lead Recommendation

READY FOR DEVELOPER EXECUTION

Proceed with a constrained, inspection-first Sprint 1 implementation. Runtime edits are permitted only if a concrete governance gap is found in the Validation Layer contract.
